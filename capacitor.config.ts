import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.speedygames.speedyarrow',
  appName: 'Speedy Arrow',
  webDir: 'dist'
};

export default config;
